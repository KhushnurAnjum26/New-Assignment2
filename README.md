------1)What is JSX, and why is it used?---------

jsx is a syntax extension for javascript that allows to write html like code using js


-------2)What is the difference between State and Props?----------------

state is the internal component that canbe changed overtime

props are data that are passed down from parent to child




--------3)What is the useState hook, and how does it work?---------------

usestate  hook allows functional components of js to have state .it has 2 parameters one states the current value of the state and the other lets the function to update states.



---------4)How can you share state between components in React?---------------

There are several ways,these are-lift state up,context api and state management libraries like redux,recoil etc.


---------5)How is event handling done in React?-----------

It is almost simmilar with js dom with minor dissimilarities like-
1)camel case is used for event names
2)a function is passed as event handler but not as string